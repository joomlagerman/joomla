name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: '👍🎉 You are just super. Thanks for your first Issue on this repo.'
        pr-message: '👍🎉 You are just super. Thanks for your first PR on this repo.'
