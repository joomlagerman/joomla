name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: '👍🎉 Du bist einfach super. Danke für deinen ersten Issue in diesem Repo.'
        pr-message: '👍🎉 Du bist einfach super. Danke für deinen ersten PR in diesem Repo.'
